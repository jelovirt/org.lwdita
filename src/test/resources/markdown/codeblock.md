# Codeblock

Code example on `for` loop:

    for i in items:
        println(i)

Fenced block:

```{.scala #foreach-example}
items.foreach(println)
```

Syntax highlighting:

```js
function add(a, b) {
  return a + b;
}
```
